{
  "name": "@sepulki/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for the Sepulki platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["sepulki", "robotics", "types"],
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*"
  ]
}
